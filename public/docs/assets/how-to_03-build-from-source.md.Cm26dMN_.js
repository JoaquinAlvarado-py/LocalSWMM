import{_ as t,o as s,c as i,ae as d}from"./chunks/framework.YL6nI5ZP.js";const m=JSON.parse('{"title":"How to Build the WASM Engine from Source","description":"","frontmatter":{},"headers":[],"relativePath":"how-to/03-build-from-source.md","filePath":"how-to/03-build-from-source.md"}'),o={name:"how-to/03-build-from-source.md"};function a(n,e,c,r,l,h){return s(),i("div",null,[...e[0]||(e[0]=[d(`<h1 id="how-to-build-the-wasm-engine-from-source" tabindex="-1">How to Build the WASM Engine from Source <a class="header-anchor" href="#how-to-build-the-wasm-engine-from-source" aria-label="Permalink to &quot;How to Build the WASM Engine from Source&quot;">​</a></h1><p>Build the OpenSWMM engine for WebAssembly with Emscripten and vcpkg, and regenerate the engine binaries in <code>public/</code>.</p><h2 id="_1-toolchain-locations" tabindex="-1">1. Toolchain locations <a class="header-anchor" href="#_1-toolchain-locations" aria-label="Permalink to &quot;1. Toolchain locations&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>Location</th><th>Version (reference)</th></tr></thead><tbody><tr><td>Emscripten SDK</td><td><code>.tools/emsdk/</code> (git-ignored)</td><td><code>emcc</code> 6.0.6</td></tr><tr><td>vcpkg</td><td><code>.tools/vcpkg/</code> (git-ignored)</td><td>2026-07-27</td></tr><tr><td>CMake</td><td>system (<code>pacman -S cmake</code>)</td><td>≥ 4.4.0 required by vcpkg baseline</td></tr><tr><td>Ninja</td><td>system</td><td>build generator (<code>-G Ninja</code>)</td></tr><tr><td>Node</td><td>system</td><td>for scripts + <code>npm install</code></td></tr></tbody></table><h3 id="install-from-scratch-arch-endeavouros" tabindex="-1">Install from scratch (Arch/EndeavourOS) <a class="header-anchor" href="#install-from-scratch-arch-endeavouros" aria-label="Permalink to &quot;Install from scratch (Arch/EndeavourOS)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pacman</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --needed</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> base-devel</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nodejs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cmake</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ninja</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> curl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> unzip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tar</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> git</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --branch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> experimental</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">rep</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">o</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> LocalSWMM</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> submodule</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --init</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --recursive</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # fetch third_party/openswmm-engine</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .tools</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --depth</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/emscripten-core/emsdk.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .tools/emsdk</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .tools/emsdk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./emsdk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> latest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./emsdk</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> activate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> latest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../..</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --depth</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/microsoft/vcpkg.git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .tools/vcpkg</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .tools/vcpkg</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./bootstrap-vcpkg.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -disableMetrics</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ../..</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><h2 id="_2-the-build-script-—-scripts-build-openswmm2d-sh" tabindex="-1">2. The build script — <code>scripts/build-openswmm2d.sh</code> <a class="header-anchor" href="#_2-the-build-script-—-scripts-build-openswmm2d-sh" aria-label="Permalink to &quot;2. The build script — \`scripts/build-openswmm2d.sh\`&quot;">​</a></h2><p>The script runs the whole build. What it does, step by step:</p><ol><li>Locates <code>.tools/emsdk</code>, <code>.tools/vcpkg</code>, source <code>cmake/wasm</code>, build dir <code>build/openswmm2d-wasm-emscripten</code>; sources <code>emsdk_env.sh</code> if <code>emcmake</code> isn&#39;t on PATH.</li><li>Guards: engine submodule present; vcpkg toolchain present.</li><li>Exports <code>VCPKG_DEFAULT_TRIPLET=wasm32-emscripten</code>, <code>VCPKG_OVERLAY_TRIPLETS=$ROOT/vcpkg-triplets</code>, <code>EMCC_SKIP_SANITY_CHECK=1</code>.</li><li><code>emcmake cmake -S cmake/wasm -B build/openswmm2d-wasm-emscripten -G Ninja</code> with flags:</li></ol><table tabindex="0"><thead><tr><th>Flag</th><th>Value</th></tr></thead><tbody><tr><td><code>CMAKE_BUILD_TYPE</code></td><td><code>Release</code></td></tr><tr><td><code>CMAKE_TOOLCHAIN_FILE</code></td><td><code>.tools/vcpkg/scripts/buildsystems/vcpkg.cmake</code></td></tr><tr><td><code>VCPKG_CHAINLOAD_TOOLCHAIN_FILE</code></td><td><code>$EMSDK/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake</code></td></tr><tr><td><code>VCPKG_TARGET_TRIPLET</code> / <code>VCPKG_DEFAULT_TRIPLET</code></td><td><code>wasm32-emscripten</code></td></tr><tr><td><code>VCPKG_MANIFEST_DIR</code></td><td>repo root</td></tr><tr><td><code>VCPKG_MANIFEST_NO_DEFAULT_FEATURES</code></td><td><code>ON</code></td></tr><tr><td><code>OPENSWMM_BUILD_2D</code></td><td><code>ON</code></td></tr><tr><td><code>OPENSWMM_FORCE_SCALAR</code></td><td><code>ON</code> (no SIMD on wasm32)</td></tr><tr><td><code>OPENSWMM_ENABLE_LTO</code></td><td><code>OFF</code> (<code>.sh</code>; the <code>.ps1</code> uses <code>ON</code> + <code>-msimd128</code>)</td></tr><tr><td><code>OPENSWMM_WITH_GEOPACKAGE</code></td><td><code>OFF</code></td></tr><tr><td><code>OPENSWMM_BUILD_GPU_PLUGIN</code></td><td><code>OFF</code></td></tr><tr><td><code>OPENSWMM_BUILD_TESTS</code></td><td><code>OFF</code></td></tr><tr><td><code>CMAKE_CROSSCOMPILING_EMULATOR</code></td><td><code>.tools/emsdk/node/*/bin/node</code> (so <code>FindOpenMP</code> can run its probe)</td></tr><tr><td><code>CMAKE_C_FLAGS</code> / <code>CMAKE_CXX_FLAGS</code></td><td><code>-fopenmp</code> (defines <code>SWMM_USE_OPENMP</code> → the solver&#39;s <code>#pragma omp</code> loops go live)</td></tr><tr><td><code>OPENSWMM_INSTALL</code></td><td><code>OFF</code></td></tr></tbody></table><p>The wrapper <code>cmake/wasm/CMakeLists.txt</code> adds <code>-pthread</code> + <code>-s PTHREAD_POOL_SIZE=4</code> (+ <code>PTHREAD_POOL_SIZE_STRICT=0</code>): the OpenMP loops map to Emscripten pthreads backed by <code>SharedArrayBuffer</code>. That makes the build <strong>browser-only</strong> — the page must be cross-origin isolated (COOP/COEP; see <code>public/_headers</code> / <code>server.py</code>). Models opt in with <code>THREADS n</code> in <code>[OPTIONS]</code> (default 1, bit-identical results); the 2D solver auto-degrades to 1 thread below <code>4·THREADS</code> triangles. A plain-Node reference run (<code>run-engine-marcher.mjs</code>) therefore needs a non-threaded build.</p><ol start="5"><li><code>cmake --build … --target openswmm2d_wasm --parallel</code> (the static <code>libopenswmm.engine.a</code> links into the modular JS/WASM pair).</li><li><strong>Copies</strong> <code>public/openswmm2d.wasm → public/swmm6wasm.wasm</code>, <code>openswmm2d.js → swmm6wasm.js</code>, and <code>openswmm2d.worker.js → swmm6wasm.worker.js</code> (legacy aliases; the worker file is only emitted if the toolchain produces one).</li><li><strong>Stamps</strong> <code>public/openswmm2d.version.json</code> + <code>public/swmm6wasm.version.json</code> with <code>engineCommit</code>, <code>engineDescribe</code> (<code>git describe --always --dirty --tags</code>), <code>builtAtUtc</code>.</li></ol><p>Run it with <code>npm run build:2d-wasm:sh</code> (Linux) or <code>npm run build:2d-wasm</code> (Windows PowerShell <code>.ps1</code>).</p><h2 id="_3-the-vcpkg-manifest-triplet" tabindex="-1">3. The vcpkg manifest &amp; triplet <a class="header-anchor" href="#_3-the-vcpkg-manifest-triplet" aria-label="Permalink to &quot;3. The vcpkg manifest &amp; triplet&quot;">​</a></h2><p>Root <code>vcpkg.json</code> deps: <strong><code>eigen3</code>, <code>hdf5</code>, <code>nlohmann-json</code>, <code>sundials</code></strong>. (HDF5 is needed by the 2D module&#39;s <code>Default2DOutputPlugin</code>; SUNDIALS is declared but the current explicit marcher no longer uses it.)</p><p><code>vcpkg-triplets/wasm32-emscripten.cmake</code> (why each line matters):</p><table tabindex="0"><thead><tr><th>Line</th><th>Meaning</th></tr></thead><tbody><tr><td><code>VCPKG_TARGET_ARCHITECTURE wasm32</code></td><td>32-bit wasm ABI</td></tr><tr><td><code>VCPKG_CRT_LINKAGE static</code> / <code>VCPKG_LIBRARY_LINKAGE static</code></td><td>static only — Emscripten links everything into one <code>.wasm</code></td></tr><tr><td><code>VCPKG_CMAKE_SYSTEM_NAME Emscripten</code></td><td>vcpkg Emscripten cross-build mode</td></tr><tr><td><code>VCPKG_ENV_PASSTHROUGH_UNTRACKED &quot;EMSDK;EMSDK_NODE;EMSDK_PYTHON;PATH&quot;</code></td><td>ports inherit the SDK env</td></tr><tr><td><code>VCPKG_CHAINLOAD_TOOLCHAIN_FILE &quot;$ENV{EMSDK}/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake&quot;</code></td><td>the actual Emscripten platform module</td></tr><tr><td><code>VCPKG_BUILD_TYPE release</code></td><td>release variants only</td></tr></tbody></table><blockquote><p><strong>Manifest subtlety:</strong> the engine submodule ships its <em>own</em> <code>vcpkg.json</code> (gtest, sqlite3, kokkos…). Because the WASM wrapper consumes the engine via <code>add_subdirectory</code>, vcpkg honors only the <strong>top-level</strong> manifest (<code>VCPKG_MANIFEST_DIR=$ROOT</code>), so the root manifest governs the WASM build. <code>VCPKG_MANIFEST_NO_DEFAULT_FEATURES=ON</code> keeps the engine&#39;s <code>2d</code>/<code>gpu</code> default features (which would drag in HDF5/Kokkos <em>again</em>) out.</p></blockquote><h2 id="_4-the-cmake-wrapper-—-cmake-wasm-cmakelists-txt" tabindex="-1">4. The CMake wrapper — <code>cmake/wasm/CMakeLists.txt</code> <a class="header-anchor" href="#_4-the-cmake-wrapper-—-cmake-wasm-cmakelists-txt" aria-label="Permalink to &quot;4. The CMake wrapper — \`cmake/wasm/CMakeLists.txt\`&quot;">​</a></h2><p>The wrapper exists because upstream removed the <code>OPENSWMM_WASM_INJECT_FILE</code> hook, so the wasm target can no longer be declared inside the engine tree. The wrapper:</p><ul><li><code>add_subdirectory(../../third_party/openswmm-engine …)</code> (same embedding the upstream Python bindings use).</li><li><code>add_executable(openswmm2d_wasm wasm/openswmm2d_exports.cpp)</code> + <code>target_link_libraries(openswmm2d_wasm PRIVATE openswmm_engine)</code>.</li><li>Output <code>openswmm2d</code> into <code>public/</code>.</li></ul><p><strong>emcc link flags</strong> (<code>cmake/wasm/CMakeLists.txt:34-48</code>) and their meaning:</p><table tabindex="0"><thead><tr><th>Flag</th><th>Meaning</th></tr></thead><tbody><tr><td><code>-s WASM=1</code></td><td>WebAssembly output</td></tr><tr><td><code>-s MODULARIZE=1</code>, <code>-s EXPORT_NAME=createOpenSwmm2D</code></td><td>wrap output in the factory function</td></tr><tr><td><code>-s EXPORT_ES6=0</code>, <code>-s ENVIRONMENT=web,worker</code></td><td>classic wrapper, usable in main thread + workers</td></tr><tr><td><code>-s ALLOW_MEMORY_GROWTH=1</code></td><td>heap can grow past <code>INITIAL_MEMORY</code></td></tr><tr><td><code>-s FILESYSTEM=1</code></td><td>MEMFS virtual FS (for <code>.inp</code>/<code>.rpt</code>/<code>.out</code>)</td></tr><tr><td><code>-s DISABLE_EXCEPTION_CATCHING=0</code></td><td>keep C++ exceptions</td></tr><tr><td><code>--no-entry</code></td><td>library module, no <code>main</code></td></tr><tr><td><code>-s EXPORTED_RUNTIME_METHODS=[&#39;cwrap&#39;,&#39;FS&#39;,&#39;getValue&#39;]</code></td><td>runtime helpers</td></tr><tr><td><code>-s EXPORTED_FUNCTIONS=[…31 symbols…]</code></td><td>exact C API surface</td></tr><tr><td><code>-s INITIAL_MEMORY=134217728</code> (128 MiB), <code>-s STACK_SIZE=5242880</code> (5 MiB)</td><td>memory sizing</td></tr><tr><td><code>-s WASM_ASYNC_COMPILATION=1</code></td><td>async compile (all callers use <code>instantiateWasm</code>)</td></tr></tbody></table><p><code>cmake/OpenSwmm2DWasm.cmake</code> is a <strong>legacy sibling</strong> of the same target (not referenced by the build scripts).</p><h2 id="_5-the-engine-submodule-the-wasm-compatibility-fix" tabindex="-1">5. The engine submodule &amp; the wasm compatibility fix <a class="header-anchor" href="#_5-the-engine-submodule-the-wasm-compatibility-fix" aria-label="Permalink to &quot;5. The engine submodule &amp; the wasm compatibility fix&quot;">​</a></h2><p><code>third_party/openswmm-engine</code> is a <strong>git submodule</strong> (<code>https://github.com/JoaquinAlvarado-py/openswmm.engine.git</code>). The <code>experimental</code> branch pins a commit that is <strong>not buildable for Emscripten out of the box</strong>: <code>src/engine/plugins/PluginFactory.cpp</code> uses <code>dlopen</code>/<code>dlsym</code> and its platform <code>#if</code> chain doesn&#39;t handle <code>__EMSCRIPTEN__</code> (<code>#error &quot;PluginFactory: unsupported platform&quot;</code>).</p><p>The fix (commit <code>85e4be38</code>, <em>&quot;fix(wasm): emscripten compatibility and build shims for web execution&quot;</em>, cherry-picked from the engine&#39;s <code>swmm6_rel</code> branch onto the pinned <code>2932a5b</code> and committed locally) makes the wasm build work by:</p><ol><li>Building <code>openswmm_engine</code> and the legacy libraries as <strong>STATIC</strong> under <code>EMSCRIPTEN</code> (<code>src/engine/CMakeLists.txt</code>, <code>src/legacy/*/CMakeLists.txt</code>).</li><li>Stripping <code>__attribute__((visibility(...)))</code> export macros (<code>include/openswmm/engine/openswmm_engine_export.h</code>, <code>openswmm_legacy_solver_export.h</code>).</li><li>Adding Emscripten no-ops to <code>PluginFactory.cpp</code>: dynamic loading disabled, <code>discover()</code> early-returns, <code>.wasm</code> treated as a shared-library extension.</li><li>Re-adding the <code>OPENSWMM_WASM_INJECT_FILE</code> include hook in the top-level <code>CMakeLists.txt</code>.</li></ol><p>The LocalSWMM repo carries commit <code>83dc0df</code> <em>&quot;submodule: pin openswmm-engine with wasm compatibility fix&quot;</em> which moves the gitlink to <code>85e4be38</code>. The engine is otherwise described as <code>v6.0.0-alpha.1-347-g85e4be38</code>.</p><blockquote><p>⚠️ <strong>If you re-pin the submodule or run <code>git submodule update</code>, keep the wasm-compat commit</strong> or the WASM build will fail again at <code>PluginFactory.cpp</code>. The prebuilt binaries in <code>public/</code> were built from <code>85e4be38</code> and are unaffected by the submodule pointer.</p></blockquote><h2 id="_6-rebuild-the-wasm-engine" tabindex="-1">6. Rebuild the WASM engine <a class="header-anchor" href="#_6-rebuild-the-wasm-engine" aria-label="Permalink to &quot;6. Rebuild the WASM engine&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/LocalSWMM</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build:2d-wasm:sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # = bash scripts/build-openswmm2d.sh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># outputs: public/openswmm2d.{js,wasm}, public/swmm6wasm.{js,wasm}, *.version.json</span></span></code></pre></div><p>First build compiles the four vcpkg ports for <code>wasm32-emscripten</code> (Eigen/HDF5/nlohmann-json/SUNDIALS) — allow 10–40 minutes. Subsequent builds use the vcpkg binary cache. The rebuilt binaries are <strong>tracked files</strong> — commit them with the engine stamp when they change.</p>`,33)])])}const k=t(o,[["render",a]]);export{m as __pageData,k as default};
