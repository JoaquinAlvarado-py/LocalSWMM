import{_ as t,o,c as a,a0 as i}from"./chunks/framework.BGyYNsHo.js";const h=JSON.parse('{"title":"Simulation Orchestration","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/hydraulics/03-simulation-orchestration.md","filePath":"explanation/hydraulics/03-simulation-orchestration.md"}'),n={name:"explanation/hydraulics/03-simulation-orchestration.md"};function r(l,e,s,d,c,u){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="simulation-orchestration" tabindex="-1">Simulation Orchestration <a class="header-anchor" href="#simulation-orchestration" aria-label="Permalink to &quot;Simulation Orchestration&quot;">​</a></h1><h2 id="simulation-orchestration-1" tabindex="-1">Simulation orchestration <a class="header-anchor" href="#simulation-orchestration-1" aria-label="Permalink to &quot;Simulation orchestration&quot;">​</a></h2><h3 id="the-overall-time-stepping-loop" tabindex="-1">The overall time-stepping loop <a class="header-anchor" href="#the-overall-time-stepping-loop" aria-label="Permalink to &quot;The overall time-stepping loop&quot;">​</a></h3><p>The engine is a single-step API. Each call to <code>SWMMEngine::step()</code> executes exactly one routing step (or one runoff-only step when routing is disabled). The sequence within a step is (<code>SWMMEngine.cpp:941</code>):</p><ol><li><p>Compute the routing time step $dt_{\\mathrm{next}}$. If the variable-step option is enabled, the Courant-limited step is</p><p>$$dt_{\\mathrm{cfl}} = \\text{router.getAdaptiveStep}(\\text{routing_step},\\ \\text{variable_step}),$$</p><p>then $dt_{\\mathrm{next}} = \\min(\\text{routing_step},\\ dt_{\\mathrm{cfl}})$ clamped to the remaining simulation duration.</p></li><li><p>Save the old hydraulic state (<code>ctx.save_state()</code>).</p></li><li><p>Reset the per-step mass-balance accumulators.</p></li><li><p>Run the pipeline:</p><ol><li><code>stepRunoff($dt$)</code> — hydrology (always);</li><li>if routing is enabled: <code>stepRouting($dt$)</code> (hydraulics + quality), <code>updateStatistics($dt$)</code>, <code>updateRoutingMassBalance($dt$)</code>;</li><li><code>computeFinalStorage()</code>.</li></ol></li><li><p>Advance the simulation clock and post output snapshots.</p></li></ol><h3 id="runoff-and-routing-clocks" tabindex="-1">Runoff and routing clocks <a class="header-anchor" href="#runoff-and-routing-clocks" aria-label="Permalink to &quot;Runoff and routing clocks&quot;">​</a></h3><p>The hydrology and hydraulics run on <em>independent</em> clocks:</p><ul><li>The <strong>runoff clock</strong> advances at the wet step (300 s default) or dry step (3600 s default), shortened to align with rain-gage boundaries. <code>stepRunoff</code> runs multiple runoff sub-steps within one routing step ($\\text{while } t_{\\mathrm{runoff}} &lt; t_{\\mathrm{routing}} + dt$).</li><li>The <strong>routing clock</strong> advances by $dt$ (the routing step) each <code>step()</code> call.</li></ul><p>After the runoff sub-steps, the wet-weather lateral inflow delivered to nodes is obtained by <em>linear interpolation</em> between the bracketing runoff evaluations: with $f = (t_{\\mathrm{elapsed}} - t_{\\mathrm{runoff,old}}) / (t_{\\mathrm{runoff,new}} - t_{\\mathrm{runoff,old}})$,</p><p>$$Q_{\\mathrm{runoff}} = (1-f),Q_{\\mathrm{runoff}}^{\\mathrm{old}} + f,Q_{\\mathrm{runoff}}^{\\mathrm{new}},$$</p><p>plus runon and groundwater contributions. These are scattered into <code>nodes.runoff_inflow</code> / <code>nodes.gw_inflow</code>.</p><h3 id="lateral-inflows" tabindex="-1">Lateral inflows <a class="header-anchor" href="#lateral-inflows" aria-label="Permalink to &quot;Lateral inflows&quot;">​</a></h3><p>Every routing step, the lateral inflows are assembled from decomposed source buffers in legacy order (<code>assembleLateralInflows</code>, <code>SWMMEngine.cpp:5691</code>):</p><p>$$Q_{\\mathrm{lat}} = Q_{\\mathrm{ext}} + Q_{\\mathrm{dwf}} + Q_{\\mathrm{wet}} + Q_{\\mathrm{gw}} + Q_{\\mathrm{rdii}} + Q_{\\mathrm{iface}} + Q_{\\mathrm{user}} + Q_{\\mathrm{coupling}},$$</p><p>where the terms are external/inflow time series, dry-weather flow, wet-weather (runoff) inflow, groundwater inflow, RDII, interface-file inflow, user-forced inflow, and 1D–2D coupling inflow respectively.</p><h3 id="the-routing-step" tabindex="-1">The routing step <a class="header-anchor" href="#the-routing-step" aria-label="Permalink to &quot;The routing step&quot;">​</a></h3><p><code>Router::step(ctx, dt, evap_rate, non_conduit_fn)</code> (<code>Routing.cpp:308</code>) performs, in order:</p><ol><li><strong>initNodeFlows</strong> — initialize each node&#39;s inflow from lateral flow and outflow from losses (storage evaporation and Green–Ampt exfiltration, jointly capped against the stored volume); set overflow from excess stored volume.</li><li><strong>computeConduitLosses</strong> — conduit evaporation (open sections) and seepage loss rates. For DYNWAVE this is instead recomputed per Picard iteration inside the solver.</li><li><strong>setAllOutfallDepths</strong> — set the outfall boundary stages (<a href="./10-structures-and-boundaries.html">Outfall boundary conditions</a>).</li><li><strong>Solver dispatch</strong> — KINWAVE, DYNWAVE, FV, or STEADY.</li><li><strong>computeDividerFlows</strong> — apply flow-divider logic.</li><li><strong>updateLinkStates</strong> — recompute node heads $H = z_{\\mathrm{inv}} + d$.</li></ol><p>Inside the dynamic-wave solver, the non-conduit callback computes pump/orifice/weir/outlet flows inside the Picard iteration, in link-index order, with immediate scatter into the node inflow/outflow accumulators.</p>',19)])])}const m=t(n,[["render",r]]);export{h as __pageData,m as default};
