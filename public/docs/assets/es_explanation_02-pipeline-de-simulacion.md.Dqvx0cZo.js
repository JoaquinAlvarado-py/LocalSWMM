import{_ as a,o as s,c as o,a0 as i}from"./chunks/framework.BL79I5mR.js";const u=JSON.parse('{"title":"El pipeline de simulación y el puente al motor WASM","description":"","frontmatter":{},"headers":[],"relativePath":"es/explanation/02-pipeline-de-simulacion.md","filePath":"es/explanation/02-pipeline-de-simulacion.md"}'),t={name:"es/explanation/02-pipeline-de-simulacion.md"};function n(r,e,d,l,c,p){return s(),o("div",null,[...e[0]||(e[0]=[i(`<h1 id="el-pipeline-de-simulacion-y-el-puente-al-motor-wasm" tabindex="-1">El pipeline de simulación y el puente al motor WASM <a class="header-anchor" href="#el-pipeline-de-simulacion-y-el-puente-al-motor-wasm" aria-label="Permalink to &quot;El pipeline de simulación y el puente al motor WASM&quot;">​</a></h1><p>Cómo fluye una simulación desde la UI al motor y de vuelta — el worker, los contratos de mensajes y el puente WASM que conecta JavaScript con el motor C++.</p><h2 id="flujo-de-extremo-a-extremo" tabindex="-1">Flujo de extremo a extremo <a class="header-anchor" href="#flujo-de-extremo-a-extremo" aria-label="Permalink to &quot;Flujo de extremo a extremo&quot;">​</a></h2><p>Presionar <strong>Run</strong> (<code>#btn-run</code>, <code>ui.js:216</code>) recorre este camino:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Botón Run (#btn-run, ui.js:216)</span></span>
<span class="line"><span>  └─ window.runSimulation()                       app.js</span></span>
<span class="line"><span>       ├─ guardas: ≥1 nodo, ≥1 OUTFALL</span></span>
<span class="line"><span>       ├─ inpText = inpExporter.generateInp(Net)</span></span>
<span class="line"><span>       ├─ targetDuration = estimateSimDurationMs(...)</span></span>
<span class="line"><span>       │</span></span>
<span class="line"><span>       └─ runSimulationInWorker(inpText, …)</span></span>
<span class="line"><span>            ├─ simWorker.js persistente (pre-calentado)</span></span>
<span class="line"><span>            └─ fallback runSimulationOnMainThread</span></span>
<span class="line"><span>            ├─ App.outData = new SWMMOutParser(out).parse()</span></span>
<span class="line"><span>            └─ displayResults(rpt, outData)              results.js</span></span></code></pre></div><h2 id="validacion-previa-al-vuelo" tabindex="-1">Validación previa al vuelo <a class="header-anchor" href="#validacion-previa-al-vuelo" aria-label="Permalink to &quot;Validación previa al vuelo&quot;">​</a></h2><p><code>runSimulation</code> aborta con una advertencia si: no hay nodos; o no hay un nodo <code>OUTFALL</code>.</p><h2 id="estimacion-de-duracion-progreso-cosmetico" tabindex="-1">Estimación de duración (progreso cosmético) <a class="header-anchor" href="#estimacion-de-duracion-progreso-cosmetico" aria-label="Permalink to &quot;Estimación de duración (progreso cosmético)&quot;">​</a></h2><p><code>estimateSimDurationMs(inpText, networkSize)</code> parsea START/END de <code>[OPTIONS]</code> y <code>ROUTING_STEP</code> del INP y extrapola el tiempo de reloj. La barra de progreso de Run Status se maneja con un <code>setInterval</code> contra esta estimación, con tope en 99% y luego asintótica a 100%.</p><h2 id="web-workers" tabindex="-1">Web Workers <a class="header-anchor" href="#web-workers" aria-label="Permalink to &quot;Web Workers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Worker</th><th>Instanciado en</th><th>Rol</th></tr></thead><tbody><tr><td><code>parseWorker.js</code></td><td><code>app.js</code> (por import)</td><td>Parsear el texto <code>.inp</code> fuera del hilo principal</td></tr><tr><td><code>simWorker.js</code></td><td><code>app.js</code> (<strong>persistente</strong>)</td><td>Corrida del motor 1D; pre-calentado en la carga</td></tr></tbody></table><p><strong>Contratos de mensajes de los workers</strong> (principal → worker):</p><ul><li><code>parseWorker</code>: <code>{ text }</code> → postea <code>{type:&#39;progress&#39;|&#39;done&#39;, model}</code> | <code>{type:&#39;error&#39;}</code>.</li><li><code>simWorker</code>: <code>{ type:&#39;run&#39;, inpText, targetDurationMs, files? }</code> → postea <code>{type:&#39;ready&#39;}</code> (una vez, tras compilar el WASM), <code>{type:&#39;log&#39;|&#39;err&#39;}</code>, <code>{type:&#39;done&#39;, rpt, outBuffer}</code> (ArrayBuffer transferible), <code>{type:&#39;error&#39;}</code>.</li></ul><p><strong>Detener:</strong> <code>stopSimulationWorker()</code> termina el worker, limpia el timer de progreso y restaura el botón Run.</p><h2 id="ejecucion-en-el-motor" tabindex="-1">Ejecución en el motor <a class="header-anchor" href="#ejecucion-en-el-motor" aria-label="Permalink to &quot;Ejecución en el motor&quot;">​</a></h2><ul><li><strong><code>simWorker.js</code></strong> — runner 1D bloqueante: escribe un <code>.inp</code> en MEMFS, llama <code>stride(engine, 10_000_000, …)</code> una vez para correr hasta completarse, lee <code>/rpt.rpt</code> y <code>/out.out</code>, transfiere los bytes.</li></ul><h2 id="resultados" tabindex="-1">Resultados <a class="header-anchor" href="#resultados" aria-label="Permalink to &quot;Resultados&quot;">​</a></h2><p><strong>Parser binario <code>.out</code> — <code>swmmOutParser.js</code>:</strong> lee el footer del archivo de salida SWMM (últimos seis INT32: <code>idNamesOffset, objPropsOffset, resultsOffset, numPeriods, errCode, magicEnd</code>), valida el magic <code>516114522</code>, parsea los headers y los conteos de variables, y expone <strong>vistas <code>Float32Array</code> zero-copy</strong> sobre los registros de periodo (<code>readResults</code>). Caminos calientes: <code>getTimeSeries(type,index,varIndex)</code> (cacheado) y <code>getStepData(type,step,varIndex)</code> para el coloreado del mapa por paso.</p><p><strong>Presentación — <code>results.js</code>:</strong> el texto <code>.rpt</code> se parsea una vez en tablas resumen (<code>parseNodeDepths</code>, <code>parseLinkFlows</code>, <code>parseFlooding</code>, <code>parseNodeInflows</code>, <code>parseOutfallLoadings</code>, <code>parseConduitSurcharges</code>, <code>parseSubcatchmentRunoffs</code>, <code>parseContinuityErrors</code>, <code>parseEngineErrors</code>, <code>parseTimeSeries</code>). <code>ResultStyling</code> es dueño del coloreado: <code>applyToMapForStep(step)</code> pinta el mapa vía la vía binaria optimizada <code>.out</code> o el fallback <code>.rpt</code>. <code>displayResults</code> construye tarjetas KPI, chips de continuidad, leyendas de color, tablas de resultados ordenables/filtrables con minigráficos lazy vía <code>IntersectionObserver</code>, y fly-to al hacer clic.</p><p><strong>Gráficos:</strong> <code>profile.js</code> <code>ProfilePlot.openForNodes</code> traza BFS por conductos, muestrea el terreno y dibuja un perfil hidráulico (código de color de capacidad: rojo ≥1.0 en carga, ámbar ≥0.85, cian normal). <code>plot.js</code> <code>TimeSeriesPlot</code> grafica series de tiempo multi-serie desde el binario <code>.out</code> (preferido) o con fallback <code>.rpt</code>.</p><h2 id="los-binarios-distribuidos" tabindex="-1">Los binarios distribuidos <a class="header-anchor" href="#los-binarios-distribuidos" aria-label="Permalink to &quot;Los binarios distribuidos&quot;">​</a></h2><p><code>public/</code> contiene el build del motor (<code>swmm6wasm.js</code> + <code>swmm6wasm.wasm</code>).</p><p>El pegamento Emscripten exporta la fábrica <strong><code>createModule</code></strong> (o <code>createOpenSwmm2D</code>).</p><h2 id="instanciacion-de-modulo" tabindex="-1">Instanciación de módulo <a class="header-anchor" href="#instanciacion-de-modulo" aria-label="Permalink to &quot;Instanciación de módulo&quot;">​</a></h2><p>El WASM se <strong>compila una vez, se instancia por corrida</strong> (~10–50 ms por re-instanciación):</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> module</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WebAssembly.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compileStreaming</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;swmm6wasm.wasm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> factory</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ noInitialRun: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    instantiateWasm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">imports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WebAssembly.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instantiate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, imports).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">m</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(m.instance)) });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> engine</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> factory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fresh instance each run</span></span></code></pre></div><h2 id="api-c-exportada-simbolos-en-exported-functions" tabindex="-1">API C exportada (símbolos en <code>EXPORTED_FUNCTIONS</code>) <a class="header-anchor" href="#api-c-exportada-simbolos-en-exported-functions" aria-label="Permalink to &quot;API C exportada (símbolos en \`EXPORTED_FUNCTIONS\`)&quot;">​</a></h2><p><strong>Ciclo de vida</strong> (<code>swmm_engine_*</code>; máquina de estados <code>CREATED → OPENED → INITIALIZED → STARTED → [RUNNING] → ENDED → CLOSED</code>):</p><table tabindex="0"><thead><tr><th>Símbolo</th><th>Significado</th></tr></thead><tbody><tr><td><code>swmm_engine_create</code> / <code>destroy</code></td><td>Asignar / liberar el handle del motor</td></tr><tr><td><code>swmm_engine_open(engine, inp, rpt, out, plugin)</code></td><td>Parsear el <code>.inp</code></td></tr><tr><td><code>swmm_engine_initialize</code> / <code>start</code> / <code>end</code> / <code>close</code></td><td>Transiciones del ciclo de vida (<code>start</code> toma <code>save_results</code>)</td></tr><tr><td><code>swmm_engine_step(engine, double* elapsed)</code></td><td>Avanzar exactamente un paso de tránsito</td></tr><tr><td><code>swmm_engine_stride(engine, n_steps, double* elapsed)</code></td><td>Avanzar hasta <code>n</code> pasos en una llamada</td></tr><tr><td><code>swmm_engine_report(engine)</code></td><td>Escribir el archivo de reporte resumen</td></tr></tbody></table><p>Más <code>malloc</code>/<code>free</code> para la gestión de memoria JS↔WASM.</p><h2 id="gestion-de-memoria" tabindex="-1">Gestión de memoria <a class="header-anchor" href="#gestion-de-memoria" aria-label="Permalink to &quot;Gestión de memoria&quot;">​</a></h2><ul><li>Todos los punteros vienen de <code>Module._malloc(bytes)</code> y se liberan con <code>Module._free(ptr)</code>.</li><li>Los archivos van al sistema de archivos virtual MEMFS de Emscripten: <code>FS.writeFile(&#39;/in.inp&#39;, inpText)</code>, <code>FS.readFile(&#39;/rpt.rpt&#39;, {encoding:&#39;utf8&#39;})</code>, <code>FS.readFile(&#39;/out.out&#39;)</code> que retorna un <code>Uint8Array</code> <strong>vista sobre el heap del WASM</strong>. El worker recorta una copia (<code>outBytes.buffer.slice(...)</code>) para que el buffer del heap pueda desprenderse en la transferencia.</li></ul>`,32)])])}const m=a(t,[["render",n]]);export{u as __pageData,m as default};
