import{_ as s,o as t,c as a,a0 as i}from"./chunks/framework.BGyYNsHo.js";const k=JSON.parse('{"title":"The Simulation Pipeline and the WASM Engine Bridge","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/02-simulation-pipeline.md","filePath":"explanation/02-simulation-pipeline.md"}'),n={name:"explanation/02-simulation-pipeline.md"};function o(r,e,d,l,p,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="the-simulation-pipeline-and-the-wasm-engine-bridge" tabindex="-1">The Simulation Pipeline and the WASM Engine Bridge <a class="header-anchor" href="#the-simulation-pipeline-and-the-wasm-engine-bridge" aria-label="Permalink to &quot;The Simulation Pipeline and the WASM Engine Bridge&quot;">​</a></h1><p>How a simulation flows from the UI to the engine and back — the worker, the message contracts, and the WASM bridge that connects JavaScript to the C++ engine.</p><h2 id="end-to-end-flow" tabindex="-1">End-to-end flow <a class="header-anchor" href="#end-to-end-flow" aria-label="Permalink to &quot;End-to-end flow&quot;">​</a></h2><p>Pressing <strong>Run</strong> (<code>#btn-run</code>, <code>ui.js:216</code>) walks this path:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Run button (#btn-run, ui.js:216)</span></span>
<span class="line"><span>  └─ window.runSimulation()                       app.js</span></span>
<span class="line"><span>       ├─ guards: ≥1 node, ≥1 OUTFALL</span></span>
<span class="line"><span>       ├─ inpText = inpExporter.generateInp(Net)</span></span>
<span class="line"><span>       ├─ targetDuration = estimateSimDurationMs(...)</span></span>
<span class="line"><span>       │</span></span>
<span class="line"><span>       └─ runSimulationInWorker(inpText, …)</span></span>
<span class="line"><span>            ├─ persistent simWorker.js (pre-warmed)</span></span>
<span class="line"><span>            └─ fallback runSimulationOnMainThread</span></span>
<span class="line"><span>            ├─ App.outData = new SWMMOutParser(out).parse()</span></span>
<span class="line"><span>            └─ displayResults(rpt, outData)              results.js</span></span></code></pre></div><h2 id="pre-flight-validation" tabindex="-1">Pre-flight validation <a class="header-anchor" href="#pre-flight-validation" aria-label="Permalink to &quot;Pre-flight validation&quot;">​</a></h2><p><code>runSimulation</code> aborts with a warning if: no nodes; or no <code>OUTFALL</code> node is present.</p><h2 id="duration-estimate-cosmetic-progress" tabindex="-1">Duration estimate (cosmetic progress) <a class="header-anchor" href="#duration-estimate-cosmetic-progress" aria-label="Permalink to &quot;Duration estimate (cosmetic progress)&quot;">​</a></h2><p><code>estimateSimDurationMs(inpText, networkSize)</code> parses <code>[OPTIONS]</code> START/END and <code>ROUTING_STEP</code> from the INP and extrapolates wall-clock time. The Run-Status progress bar is driven by a <code>setInterval</code> against this estimate, capped at 99% then asymptoting to 100%.</p><h2 id="web-workers" tabindex="-1">Web Workers <a class="header-anchor" href="#web-workers" aria-label="Permalink to &quot;Web Workers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Worker</th><th>Instantiated at</th><th>Role</th></tr></thead><tbody><tr><td><code>parseWorker.js</code></td><td><code>app.js</code> (per import)</td><td>Parse <code>.inp</code> text off-thread</td></tr><tr><td><code>simWorker.js</code></td><td><code>app.js</code> (<strong>persistent</strong>)</td><td>1D engine run; pre-warmed at load</td></tr></tbody></table><p><strong>Worker message contracts</strong> (main → worker):</p><ul><li><code>parseWorker</code>: <code>{ text }</code> → posts <code>{type:&#39;progress&#39;|&#39;done&#39;, model}</code> | <code>{type:&#39;error&#39;}</code>.</li><li><code>simWorker</code>: <code>{ type:&#39;run&#39;, inpText, targetDurationMs, files? }</code> → posts <code>{type:&#39;ready&#39;}</code> (once, after WASM compile), <code>{type:&#39;log&#39;|&#39;err&#39;}</code>, <code>{type:&#39;done&#39;, rpt, outBuffer}</code> (transferable ArrayBuffer), <code>{type:&#39;error&#39;}</code>.</li></ul><p><strong>Stop:</strong> <code>stopSimulationWorker()</code> terminates the worker, clears the progress timer, and restores the Run button.</p><h2 id="engine-execution" tabindex="-1">Engine execution <a class="header-anchor" href="#engine-execution" aria-label="Permalink to &quot;Engine execution&quot;">​</a></h2><ul><li><strong><code>simWorker.js</code></strong> — 1D blocking runner: writes <code>.inp</code> to MEMFS, calls <code>stride(engine, 10_000_000, …)</code> once to run to completion, reads <code>/rpt.rpt</code> and <code>/out.out</code>, transfers the bytes.</li></ul><h2 id="results" tabindex="-1">Results <a class="header-anchor" href="#results" aria-label="Permalink to &quot;Results&quot;">​</a></h2><p><strong><code>.out</code> binary parser — <code>swmmOutParser.js</code>:</strong> reads the SWMM output-file footer (last six INT32s: <code>idNamesOffset, objPropsOffset, resultsOffset, numPeriods, errCode, magicEnd</code>), validates magic <code>516114522</code>, parses headers and variable counts, and exposes <strong>zero-copy <code>Float32Array</code> views</strong> over period records (<code>readResults</code>). Hot paths: <code>getTimeSeries(type,index,varIndex)</code> (cached) and <code>getStepData(type,step,varIndex)</code> for per-step map coloring.</p><p><strong>Presentation — <code>results.js</code>:</strong> <code>.rpt</code> text is parsed once into summary tables (<code>parseNodeDepths</code>, <code>parseLinkFlows</code>, <code>parseFlooding</code>, <code>parseNodeInflows</code>, <code>parseOutfallLoadings</code>, <code>parseConduitSurcharges</code>, <code>parseSubcatchmentRunoffs</code>, <code>parseContinuityErrors</code>, <code>parseEngineErrors</code>, <code>parseTimeSeries</code>). <code>ResultStyling</code> owns coloring: <code>applyToMapForStep(step)</code> paints the map based on the binary <code>.out</code> path or <code>.rpt</code> fallback. <code>displayResults</code> builds KPI cards, continuity chips, color legends, sortable/filterable result tables with <code>IntersectionObserver</code> lazy sparklines, and fly-to-on-click.</p><p><strong>Plotting:</strong> <code>profile.js</code> <code>ProfilePlot.openForNodes</code> BFS-traces conduits, samples terrain, and draws a hydraulic profile (capacity color coding: red ≥1.0 surcharged, amber ≥0.85, cyan normal). <code>plot.js</code> <code>TimeSeriesPlot</code> charts multi-series time series from binary <code>.out</code> (preferred) or <code>.rpt</code> fallback.</p><h2 id="the-shipped-binaries" tabindex="-1">The shipped binaries <a class="header-anchor" href="#the-shipped-binaries" aria-label="Permalink to &quot;The shipped binaries&quot;">​</a></h2><p><code>public/</code> contains the engine build (<code>swmm6wasm.js</code> + <code>swmm6wasm.wasm</code>).</p><p>The Emscripten glue exports the factory <strong><code>createModule</code></strong>.</p><h2 id="module-instantiation" tabindex="-1">Module instantiation <a class="header-anchor" href="#module-instantiation" aria-label="Permalink to &quot;Module instantiation&quot;">​</a></h2><p>The WASM binary is <strong>compiled once, instantiated per run</strong> (~10–50 ms per re-instantiation):</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> module</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WebAssembly.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compileStreaming</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;swmm6wasm.wasm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// once</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> factory</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createModule</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ noInitialRun: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    instantiateWasm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">imports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> WebAssembly.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">instantiate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, imports).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">m</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> cb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(m.instance)) });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> engine</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> factory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fresh instance each run</span></span></code></pre></div><h2 id="exported-c-api-symbols-in-exported-functions" tabindex="-1">Exported C API (symbols in <code>EXPORTED_FUNCTIONS</code>) <a class="header-anchor" href="#exported-c-api-symbols-in-exported-functions" aria-label="Permalink to &quot;Exported C API (symbols in \`EXPORTED_FUNCTIONS\`)&quot;">​</a></h2><p><strong>Lifecycle</strong> (<code>swmm_engine_*</code>; state machine <code>CREATED → OPENED → INITIALIZED → STARTED → [RUNNING] → ENDED → CLOSED</code>):</p><table tabindex="0"><thead><tr><th>Symbol</th><th>Meaning</th></tr></thead><tbody><tr><td><code>swmm_engine_create</code> / <code>destroy</code></td><td>Allocate / free the engine handle</td></tr><tr><td><code>swmm_engine_open(engine, inp, rpt, out, plugin)</code></td><td>Parse the <code>.inp</code></td></tr><tr><td><code>swmm_engine_initialize</code> / <code>start</code> / <code>end</code> / <code>close</code></td><td>Lifecycle transitions (<code>start</code> takes <code>save_results</code>)</td></tr><tr><td><code>swmm_engine_step(engine, double* elapsed)</code></td><td>Advance exactly one routing step</td></tr><tr><td><code>swmm_engine_stride(engine, n_steps, double* elapsed)</code></td><td>Advance up to <code>n</code> steps in one call</td></tr><tr><td><code>swmm_engine_report(engine)</code></td><td>Write the summary report file</td></tr></tbody></table><p>Plus <code>malloc</code>/<code>free</code> for JS↔WASM memory management.</p><h2 id="memory-management" tabindex="-1">Memory management <a class="header-anchor" href="#memory-management" aria-label="Permalink to &quot;Memory management&quot;">​</a></h2><ul><li>All pointers come from <code>Module._malloc(bytes)</code> and are freed with <code>Module._free(ptr)</code>.</li><li>Files go to the Emscripten MEMFS virtual filesystem: <code>FS.writeFile(&#39;/in.inp&#39;, inpText)</code>, <code>FS.readFile(&#39;/rpt.rpt&#39;, {encoding:&#39;utf8&#39;})</code>, <code>FS.readFile(&#39;/out.out&#39;)</code> returning a <code>Uint8Array</code> <strong>view on the WASM heap</strong>. The worker slices a copy out (<code>outBytes.buffer.slice(...)</code>) so the heap buffer can be detached on transfer.</li></ul>`,32)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
